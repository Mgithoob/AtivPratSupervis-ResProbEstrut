a partir dos documentos do professor, 
todas as exerc de lista encadeada simples já estão feitos.

EX 1.
Implementar pilha usando a lista encadeada de forma dinâmica.



EX 2.
Implementar fila usando lista encadeada de forma dinâmica.


EX 3.
Criar uma operação Merge que irá juntar o conteudo de duas listas(A,B) em uma terceira(C), de forma que esteja tudo ordenado.
- As filas A,B estarão ordenadas

Deve-se implementar com vetores e listas encadeadas.

------

// Criacao terminou
if posC > TamanhoC:
	break;	

if A[posA] <= B[posB]:
	C[posC] = [posA];
	posC++; posA++;
	
else:
	C[posC] = [posB];
	posC++; posB++;

